classdef MEIAnalysis < AnalysisTree

methods
    function obj = MEIAnalysis(cellData, dataSetName, params)
    end

    function obj = doAnalysis(obj, cellData)
%         rootData = obj.get(1);
%         leafIDs = obj.findleaves();
%         L = length(leafIDs);
%         baseline = zeros(1,L);
%         
        
    end
end

end